Docker can be used to use the tools in dev environment (docker compose) or to scale up with containers in test environment (docker swarm).

Docker Compose:
```
docker compose up -d

docker compose down
```

Docker Swarm:
